import React from "react";

const NotAllowed = () => (
    <div>
      <h2>Error 403 - Acceso prohibido</h2>
      <p>No tienes permiso para acceder a esta página.</p>
    </div>
  );

export default NotAllowed